import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  min-height: 500px;
`;

const Documents = () => {
  return <Container>사내문서</Container>;
};

export default Documents;
