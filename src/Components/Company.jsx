import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  min-height: 500px;
`;
const Company = () => {
  return <Container>회사정보</Container>;
};

export default Company;
